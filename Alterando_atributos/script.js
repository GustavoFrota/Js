var title = document.querySelector('#title');

title.setAttribute("class", "testando-atributo"); // serve para definir ou alterar um atributo de um elemento HTML no DOM

console.log(title);

var btn = document.querySelector('#btn');

btn.setAttribute("disabled", "disabled");